<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiApp2.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiApp2"
    Shell.FlyoutBehavior="Flyout"
    Title="MauiApp2">

    <!-- Page 1 -->
    <FlyoutItem Title="Page 1">
        <Tab Title="Name">
            <ShellContent
                Title="Ramji"
                ContentTemplate="{DataTemplate local:ContentPageDemo}"/>
            <ShellContent
                Title="Dinesh"
                ContentTemplate="{DataTemplate local:CommandControlDemo}"/>
        </Tab>
        <Tab Title="Fruits">
            <ShellContent
                Title="App"
                ContentTemplate="{DataTemplate local:InputControlDemo}"/>
            <ShellContent
                Title="Octane"
                ContentTemplate="{DataTemplate local:perfectpay}"/>
        </Tab>
    </FlyoutItem>

    <!-- Page 2 -->
    <FlyoutItem Title="Page 2">
        <Tab Title="H1">
            <ShellContent
                Title="Home 1"
                ContentTemplate="{DataTemplate local:MainPage}"/>
            <ShellContent
                Title="Home 2"
                ContentTemplate="{DataTemplate local:ColorMakerDemo}" />
        </Tab>
        <Tab Title="Settings">
            <ShellContent
                Title="Con 1"
                ContentTemplate="{DataTemplate local:CollectionControlsDemo}"/>
            <ShellContent
                Title="Con 2"
                ContentTemplate="{DataTemplate local:FlexLayoutPageDemo}" />
        </Tab>
    </FlyoutItem>

</Shell>
